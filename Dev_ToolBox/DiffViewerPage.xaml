<Page x:Class="Dev_ToolBox.DiffViewerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Dev_ToolBox"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Diff Viewer Page">
    <StackPanel Margin="10">
        <TextBlock Text="Percentage Calculator" FontWeight="Bold" FontSize="16" HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="txtLeftText" Grid.Column="0" TextWrapping="Wrap" AcceptsReturn="True" />
            <TextBox x:Name="txtRightText" Grid.Column="1" TextWrapping="Wrap" AcceptsReturn="True" />
            <Button Content="Compare" Click="CompareButton_Click" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,10"/>
            <TextBox x:Name="txtDiffResult" Grid.ColumnSpan="2" VerticalScrollBarVisibility="Auto" IsReadOnly="True" TextWrapping="Wrap"/>
        </Grid>
    </StackPanel>
</Page>
